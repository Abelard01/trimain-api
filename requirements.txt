# Minimal dependencies for the FastAPI inference server (used in Docker).
# Torch is installed separately in the Dockerfile to use the CPU-only wheel.
# For notebook/training dependencies, see requirements.txt.

gdown>=5.1.0
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pillow>=10.0.0
numpy>=1.24.0
open_clip_torch>=2.24.0
albumentations>=1.3.0
transformers>=4.30.0
